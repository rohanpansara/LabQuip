<nav class="navbar navbar-dark fixed-top bg-dark flex-md-nowrap p-0 pr-3">
    <a class="navbar-brand col-1 col-md-2 mr-0" href="/">
        <img class="navbar-brand__logo pl-2" src="/images/logo.png">
    </a>
    <button class="btn btn-secondary navbar-toggler d-md-none" type="button" data-toggle="collapse"
        data-target="#sideBar">
        <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" viewBox="0 0 30 30" role="img" focusable="false">
            <title>Menu</title>
            <path stroke="currentColor" stroke-linecap="round" stroke-miterlimit="10" stroke-width="2"
                d="M4 7h22M4 15h22M4 23h22" />
        </svg>
    </button>

    <ul class="navbar-nav flex-row w-100 justify-content-center
            justify-content-md-end align-items-center text-warning py-2 bg-md-light">
        <li class="nav-item mr-2 font-weight-bold"><i class="ic-calendar"></i>&nbsp;
            <% const date=new Date(); %>
                <%= date.toLocaleDateString(); %>
        </li>
        <li class="nav-item mr-2">
            |
        </li>
        <% if(user.role == 'servicing') { %>
        <li class="nav-item mr-2 font-weight-bold text-info"><i class="ic-user"></i>&nbsp;
            <%= user.name %>
        </li>
        <% } else { %>
        <li class="nav-item mr-2 font-weight-bold text-warning"><i class="ic-user"></i>&nbsp;
            <%= user.name %>
        </li>
        <% } %>
        <li class="nav-item mr-2">
            |
        </li>
        <li class="nav-item">
            <form action="/logout">
                <button class="btn-logout btn-sm" type="submit">
                    <i class="ic-share-alternitive"></i>&nbsp; &nbsp;LOGOUT
                </button>
            </form>
        </li>
    </ul>
</nav>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-2 bg-light navbar navbar-expand-md p-0 mt-5
        mt-md-0 overflow-hidden position-fixed z-100">
            <nav class="collapse navbar-collapse sidebar flex-column align-items-md-start " id="sideBar">
                <ul class="nav flex-column align-items-center align-items-md-start">
                    <% if(user.role == 'admin' || 'servicing') { %>
                    <li class="nav-item">
                        <a class="nav-link<% if(page==" Dashboard"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/dashboard">
                                    <i class="ic-home"></i>
                                    Dashboard
                        </a>
                    </li>
                    <% } %>
                    <% if(user.role == 'admin') { %>
                    <li class="nav-item">
                        <a class="nav-link <% if(page==" Products"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/products">
                                    <i class="ic-shopping-cart"></i>
                                    Products
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link<% if(page==" Inventories"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/inventories">
                                    <i class="ic-archive"></i>
                                    Inventories
                        </a>
                    </li>
                    <!-- <li class="nav-item">
                        <a class="nav-link <% if(page==" Customers"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/customers">
                                    <i class="ic-v-card"></i>
                                    Suppliers
                        </a>
                    </li>  -->
                    <!-- <li class="nav-item">
                        <a class="nav-link <% if(page==" Sales"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/sales">
                                    <i class="ic-calculator"></i>
                                    Warranty
                        </a>
                    </li> -->
                    <!-- <li class="nav-item">
                        <a class="nav-link <% if(page==" Returns"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/returns">
                                    <i class="ic-back-in-time"></i>
                                    Returns
                        </a>
                    </li>-->
                    
                    <li class="nav-item">
                        <a class="nav-link <% if(page==" Expenses"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/expenses">
                                    <i class="ic-credit-card"></i>
                                    Expenses
                        </a>
                    </li>
                    <% } %>
                    <% if(user.role == 'lab-incharge' || 'admin') { %>
                    <li class="nav-item">
                        <a class="nav-link <% if(page==" Servicing"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/servicing">
                                    <i class="ic-tools"></i>
                                    Complaints
                        </a>
                    </li>
                    <% } %>
                    
                </ul>
                <% if(user.role == 'admin') { %>
                <h6 class="sidebar-heading px-3 mt-4 mb-3 text-center
            text-muted">
                    <span class="text-center text-md-left">Access Control</span>
                </h6>
                <ul class="nav flex-column mb-2 align-items-center align-items-md-start">
                    <li class="nav-item">
                        <a class="nav-link <% if(page==" Users"){%>
                            <%= "active" ; %>
                                <% } %>"
                                    href="/users">
                                    <i class="ic-user"></i>
                                    Users
                        </a>
                    </li>
                </ul>
                <% } %>
            </nav>
        </div>


        <main role="main" class="content-area col-md-9 ml-sm-auto col-lg-10 px-2 px-md-4">